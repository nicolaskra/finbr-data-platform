name: 'finbr'
version: '0.1.0'
config-version: 2

profile: 'finbr'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  finbr:
    staging:
      # Materializado como TABLE (nao view): persiste no DuckDB para que
      # consumers (FastAPI, dashboard) nao dependam do path raw em runtime.
      # Trade-off: ocupa storage no warehouse (~14 MB/mes), ganha desacoplamento.
      +materialized: table
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      core:
        +schema: core
      analytics:
        +schema: analytics

vars:
  # Caminho dos dados raw (relativo a project root; via DuckDB read_parquet)
  raw_path: '/opt/airflow/data/raw'
  # Sobrescrevivel via --vars '{raw_path: ./data/raw}' (dev local)
